{% extends 'baseEmail.html.twig' %}

{% block body %}
    <h1>New Episode "{{ episode.title }}" created</h1>
    <p>Hello, the new Episode "{{ episode.title }}" has been created on Wild Series pour la saison {{ episode.season.number }} de la série {{ episode.season.program.title }}</p>
    <p><a href="{{ path('program_episode_show', { programSlug: episode.season.program.slug, seasonId: episode.season.id, episodeSlug: episode.slug }) }}">Retrouvez le sur Wild series !!!</a></p>
    <p>Synopsis : {{episode.synopsis }}</p>
{% endblock %}
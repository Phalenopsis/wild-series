{% extends 'base.html.twig' %}

{% block title %}Série #{{ program.id }}{% endblock %}

{% block body %}
    <div class="program card text-center">
        <div class="card-header">
            {{ program.category.name }}
        </div>
        <div class="card-body">
            <h5 class="card-title">{{ program.title }}</h5>
            <p class="card-text">{{ program.synopsis }}</p>
        </div>
        <div class="card-footer text-muted">
            {{ program.country }} - {{ program.year }}
        </div>
    </div>

    {% for season in program.seasons %}
        {% if loop.index % 2 == 1 %}
    <div class="program row">
        {% endif %}
        <div class="col-sm-6">
            <div class="program card">
                <div class="card-body">
                    <h5 class="card-title">Season {{ season.number }} - {{ season.year }}</h5>
                    <p class="card-text">{{ season.description }}</p>
                    <a href="{{ path('program_season_show', { programId: program.id, seasonId: season.id }) }}" class="btn btn-primary">View Episodes</a>
                </div>
            </div>
        </div>
        {% if loop.index % 2 == 0 %}
            </div>
        {% endif %}
    {% endfor %}
    {% if program.seasons|length % 2 == 1 %}
        </div>
    {% endif %}

{#    <div class="media">#}
{#        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">#}
{#        <div class="media-body">#}
{#            <h1 class="mt-0">{{ program.title }}</h1>#}
{#            <p>{{ program.synopsis }}</p>#}
{#            <p>{{ program.country }} - {{ program.year }}</p>#}
{#            <p>Catégorie : {{ program.category.name }}</p>#}
{#            <h2>Liste des saisons</h2>#}
{#            <ul>#}
{#            {% for season in program.seasons %}#}
{#                <li><a href="{{ path('program_season_show', { programId: program.id, seasonId: season.id }) }}">Season {{ season.number }}, {{ season.year }}</li>#}

{#            {% endfor %}#}
{#            </ul>#}
{#        </div>#}
{#    </div>#}
{% endblock %}